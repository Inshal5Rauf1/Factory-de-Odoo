[options]
addons_path = /mnt/extra-addons
data_dir = /var/lib/odoo
db_host = db
db_port = 5432
db_user = odoo
db_password = False
admin_passwd = False
; Set db_password and admin_passwd via environment variables or CLI flags.
without_demo = all
; XML-RPC is enabled by default on port 8069, no extra config needed
list_db = False
db_name = odoo_dev

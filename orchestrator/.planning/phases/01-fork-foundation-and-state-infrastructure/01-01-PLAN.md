---
phase: 01-fork-foundation-and-state-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/ -> amil/ (directory rename)
  - amil/bin/amil-tools.cjs (renamed from gsd-tools.cjs)
  - amil/bin/lib/core.cjs (MODEL_PROFILES keys)
  - amil/bin/lib/config.cjs (.gsd/ path references)
  - amil/bin/install.js (full rewrite)
  - commands/amil/ (renamed from commands/gsd/)
  - agents/*.md (12 files renamed gsd-* to amil-*)
  - hooks/*.md (3 files renamed)
  - workflows/*.md (12+ files, internal references)
  - references/*.md (internal references)
  - templates/*.md (internal references)
  - package.json
  - CLAUDE.md
  - tests/helpers.cjs
autonomous: true
requirements:
  - FORK-01
  - FORK-02
  - FORK-03
  - FORK-04
  - FORK-05
  - FORK-06
  - FORK-07
  - FORK-08
  - FORK-09
  - FORK-10
  - TEST-05

must_haves:
  truths:
    - "Running `node amil/bin/amil-tools.cjs` executes without error"
    - "grep -r 'get-shit-done\\|/amil:\\|gsd-tools' finds zero matches in codebase (excluding node_modules, .git)"
    - "All 16 existing tests pass with `npm test` after rename"
    - "install.js is Claude-Code-only and checks for amil + Python 3.8+"
  artifacts:
    - path: "amil/bin/amil-tools.cjs"
      provides: "Renamed CLI entry point"
    - path: "amil/bin/install.js"
      provides: "Claude-Code-only installer with amil and Python checks"
    - path: "amil/bin/lib/core.cjs"
      provides: "MODEL_PROFILES with amil-* keys"
    - path: "commands/amil/"
      provides: "32 command files with /amil: prefix"
    - path: "CLAUDE.md"
      provides: "Odoo ERP orchestrator context document"
    - path: "package.json"
      provides: "Renamed package with amil identity"
    - path: "tests/helpers.cjs"
      provides: "Updated TOOLS_PATH pointing to amil"
  key_links:
    - from: "tests/helpers.cjs"
      to: "amil/bin/amil-tools.cjs"
      via: "TOOLS_PATH constant"
      pattern: "amil.*amil-tools"
    - from: "amil/bin/amil-tools.cjs"
      to: "amil/bin/lib/*.cjs"
      via: "require() calls"
      pattern: "require.*lib/"
    - from: "workflows/*.md"
      to: "amil/bin/amil-tools.cjs"
      via: "Tool invocation paths"
      pattern: "amil-tools"
---

<objective>
Rename the entire Amil fork to amil: directory structure, CLI entry point, command prefix, agent names, workflow references, documentation, and package identity. Rewrite install.js for Claude Code only with amil and Python checks.

Purpose: Establish the amil identity as the foundation for all subsequent Phase 1 work. Every other plan depends on correct paths and command names.
Output: A fully renamed codebase where all 16 existing tests pass and zero old references remain.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-foundation-and-state-infrastructure/01-RESEARCH.md

@tests/helpers.cjs
@get-shit-done/bin/lib/core.cjs
@get-shit-done/bin/gsd-tools.cjs
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directory structure and all internal references</name>
  <files>
    get-shit-done/ -> amil/
    amil/bin/gsd-tools.cjs -> amil/bin/amil-tools.cjs
    commands/gsd/ -> commands/amil/
    agents/gsd-*.md -> agents/amil-*.md
    hooks/gsd-*.md -> hooks/amil-*.md
    amil/bin/lib/core.cjs
    amil/bin/lib/config.cjs
    amil/bin/lib/commands.cjs
    amil/bin/lib/state.cjs
    amil/bin/lib/phase.cjs
    amil/bin/lib/init.cjs
    amil/workflows/*.md
    amil/references/*.md
    amil/templates/*.md
    tests/helpers.cjs
    package.json
  </files>
  <action>
    Execute the rename in strict layers, verifying each layer with grep before proceeding:

    **Layer 1 - Directory rename:**
    - `git mv get-shit-done amil` (single commit preserving history)
    - `git mv commands/gsd commands/amil`

    **Layer 2 - CLI entry point rename:**
    - `git mv amil/bin/gsd-tools.cjs amil/bin/amil-tools.cjs`

    **Layer 3 - Agent and hook file renames:**
    - Rename all 12 agent files: `gsd-*.md` -> `amil-*.md` in agents/
    - Rename all 3 hook files: `gsd-*.md` -> `amil-*.md` in hooks/

    **Layer 4 - Internal reference updates (sed across all .cjs, .md, .js files):**
    - In core.cjs: Update all 12 MODEL_PROFILES keys from `gsd-*` to `amil-*`
    - In config.cjs: Update `.gsd/` references to `.amil/` (brave_api_key path, defaults.json path)
    - In amil-tools.cjs: Update `gsd-tools` references, require paths
    - In all lib/*.cjs files: Update `get-shit-done` path references to `amil`
    - In all workflow .md files: Update `gsd-tools.cjs` to `amil-tools.cjs`, `/amil:` to `/amil:`
    - In all command .md files (commands/amil/): Update frontmatter prefix from `/amil:` to `/amil:`
    - In all agent .md files: Update path references and command references
    - In all template .md files: Update tool invocation paths

    **Layer 5 - Test infrastructure:**
    - Update `tests/helpers.cjs`: Change TOOLS_PATH to `path.join(__dirname, '..', 'amil', 'bin', 'amil-tools.cjs')`
    - Update temp directory prefix from `gsd-test-` to `amil-test-`

    **Layer 6 - Package identity:**
    - Update `package.json`: name to `amil`, description to "Odoo ERP module orchestrator for Claude Code", remove multi-runtime keywords, update repository URL

    **Verification after each layer:** Run `grep -r "get-shit-done\|gsd-tools\|/amil:" --include="*.cjs" --include="*.md" --include="*.js" --include="*.json" . | grep -v node_modules | grep -v .git` and fix any remaining references.

    **CRITICAL anti-pattern to avoid:** Do NOT create double-renames (e.g., `odoo-amil`). When replacing `gsd` with `amil`, use exact string matching not regex wildcards. Replace `get-shit-done` with `amil`, `/amil:` with `/amil:`, `gsd-tools` with `amil-tools`, `gsd-planner` with `amil-planner`, etc. -- always full token matches.
  </action>
  <verify>
    <automated>grep -r "get-shit-done\|gsd-tools\.cjs\|/amil:\|\"gsd-" --include="*.cjs" --include="*.md" --include="*.js" --include="*.json" . | grep -v node_modules | grep -v ".git/" | wc -l</automated>
    Result must be 0. Then: `npm test` -- all 16 existing tests must pass.
  </verify>
  <done>Zero remaining old references. All 16 existing tests pass. Directory structure matches research architecture diagram (amil/bin/, commands/amil/, agents/amil-*.md).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite install.js and CLAUDE.md</name>
  <files>
    amil/bin/install.js
    CLAUDE.md
  </files>
  <action>
    **install.js rewrite (target ~300 LOC, down from 2464):**

    Strip ALL non-Claude-Code runtime support. The new install.js should:
    1. Detect Claude Code installation (`~/.claude/` directory exists)
    2. Create symlink or copy `amil/` to `~/.claude/amil/`
    3. Register slash commands from `commands/amil/` directory
    4. Check for amil at `~/.claude/amil/` -- warn if missing with install instructions (FORK-06)
    5. Check for Python 3.8+ via `python3 --version` -- warn if missing (FORK-07)
    6. Validate that required directories exist
    7. Print success summary with available commands

    Do NOT preserve: OpenCode detection/config, Gemini CLI detection/config, Codex CLI detection/config, multi-runtime settings management.

    Hard-delete all conditional runtime branches. Start clean from the Claude Code detection path only.

    **CLAUDE.md rewrite:**

    Create a new CLAUDE.md as the Odoo ERP orchestrator context document containing:
    1. Project identity: amil -- Odoo ERP module orchestrator built on Claude Code
    2. Command reference: all `/amil:*` commands with brief descriptions
    3. Architecture: state files (.planning/model_registry.json, module_status.json, config.json), module lifecycle (planned -> spec_approved -> generated -> checked -> shipped)
    4. Rules: sequential generation only, all state changes through CLI subcommands, atomic writes for JSON state, immutable data patterns
    5. File locations: where each state file lives, what each lib module does
    6. Development: how to run tests, coverage requirements (80%+), CJS module format, zero npm runtime dependencies
  </action>
  <verify>
    <automated>node -c amil/bin/install.js && wc -l amil/bin/install.js | awk '{if ($1 > 500) exit 1; else print "OK: " $1 " lines"}'</automated>
    install.js must parse without syntax errors and be under 500 lines. CLAUDE.md must exist.
  </verify>
  <done>install.js is Claude-Code-only (~300 LOC), checks for amil and Python 3.8+. CLAUDE.md documents the amil architecture, commands, and rules. No references to OpenCode, Gemini, or Codex remain in install.js.</done>
</task>

</tasks>

<verification>
1. `grep -r "get-shit-done\|gsd-tools\|/amil:\|\"gsd-" --include="*.cjs" --include="*.md" --include="*.js" --include="*.json" . | grep -v node_modules | grep -v .git` returns zero matches (FORK-10)
2. `npm test` passes all 16 existing tests (TEST-05)
3. `node amil/bin/amil-tools.cjs --help` or basic invocation works
4. `node -c amil/bin/install.js` parses without error
5. `test -f CLAUDE.md` confirms CLAUDE.md exists
</verification>

<success_criteria>
- Zero references to old names in codebase (get-shit-done, /amil:, gsd-tools, gsd-planner etc.)
- All 16 existing tests pass with `npm test`
- install.js is Claude-Code-only, under 500 LOC, checks amil + Python 3.8+
- CLAUDE.md documents amil architecture and commands
- package.json has amil name and description
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-foundation-and-state-infrastructure/01-01-SUMMARY.md`
</output>

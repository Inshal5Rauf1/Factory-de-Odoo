---
phase: 02-prd-decomposition-and-erp-init
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - commands/amil/new-erp.md
  - amil/workflows/new-erp.md
  - agents/amil-erp-decomposer.md
  - agents/amil-module-researcher.md
autonomous: true
requirements: [NWRP-01, NWRP-02, NWRP-03, NWRP-07]

must_haves:
  truths:
    - "Running /amil:new-erp triggers the new-erp workflow"
    - "Workflow asks 7 sequential config questions and writes each answer to config.json immediately"
    - "Workflow checks for .planning/PRD.md and prompts user if missing"
    - "4 parallel Task() agents are spawned with PRD text and existing_modules from config"
    - "Agent 1 writes module-boundaries.json, Agent 2 writes oca-analysis.json, Agent 3 writes dependency-map.json, Agent 4 writes computation-chains.json"
    - "Two dedicated agent files exist in agents/ directory"
  artifacts:
    - path: "commands/amil/new-erp.md"
      provides: "Slash command entry point"
      contains: "amil:new-erp"
    - path: "amil/workflows/new-erp.md"
      provides: "Full workflow with Stage A (questions) and Stage B (agents)"
      min_lines: 150
    - path: "agents/amil-erp-decomposer.md"
      provides: "Module boundary analysis agent"
      contains: "module-boundaries.json"
    - path: "agents/amil-module-researcher.md"
      provides: "OCA registry checker agent"
      contains: "oca-analysis.json"
  key_links:
    - from: "commands/amil/new-erp.md"
      to: "amil/workflows/new-erp.md"
      via: "execution_context reference"
      pattern: "workflows/new-erp.md"
    - from: "amil/workflows/new-erp.md"
      to: "agents/amil-erp-decomposer.md"
      via: "Task() subagent_type"
      pattern: "amil-erp-decomposer"
    - from: "amil/workflows/new-erp.md"
      to: "agents/amil-module-researcher.md"
      via: "Task() subagent_type"
      pattern: "amil-module-researcher"
---

<objective>
Create the `/amil:new-erp` command, workflow (Stage A: config questions + Stage B: 4 parallel agents), and 2 dedicated agent files.

Purpose: This is the primary entry point for initializing an Odoo ERP project. Stage A collects Odoo-specific config, Stage B analyzes the PRD with 4 specialized agents that produce JSON outputs for the merge step (Plan 03).

Output: Working command that collects config and spawns 4 agents to analyze a PRD.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/amil/workflows/execute-plan.md
@/home/inshal-rauf/.claude/amil/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prd-decomposition-and-erp-init/02-CONTEXT.md
@.planning/phases/02-prd-decomposition-and-erp-init/02-RESEARCH.md
@.planning/phases/02-prd-decomposition-and-erp-init/02-01-SUMMARY.md

<interfaces>
<!-- Existing command pattern from commands/amil/new-project.md -->
```markdown
---
name: amil:new-project
description: Initialize a new project with deep context gathering and PROJECT.md
argument-hint: "[--auto]"
allowed-tools:
  - Read
  - Bash
  - Write
  - Task
  - AskUserQuestion
---
<execution_context>
@~/.claude/amil/workflows/new-project.md
</execution_context>
<process>
Execute the new-project workflow end-to-end.
</process>
```

<!-- Config CLI pattern -->
```bash
node "$HOME/.claude/amil/bin/amil-tools.cjs" config-set odoo.version "17.0"
node "$HOME/.claude/amil/bin/amil-tools.cjs" config-set odoo.multi_company false
```

<!-- Task() spawning pattern (from new-project.md workflow) -->
```
Task(prompt="...", subagent_type="amil-project-researcher", model="{model}", description="Stack research")
```

<!-- Agent output schemas (from CONTEXT.md) -->
module-boundaries.json: { "modules": [{ "name", "description", "models", "base_depends", "estimated_complexity" }] }
oca-analysis.json: { "findings": [{ "domain", "oca_module", "odoo_module", "recommendation", "reason" }] }
dependency-map.json: { "dependencies": [{ "module", "depends_on", "reason" }] }
computation-chains.json: { "chains": [{ "name", "description", "steps", "cross_module" }] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create new-erp command file and 2 agent files</name>
  <files>commands/amil/new-erp.md, agents/amil-erp-decomposer.md, agents/amil-module-researcher.md</files>
  <action>
    1. Create `commands/amil/new-erp.md` following the exact pattern from `new-project.md`:
       - name: `amil:new-erp`
       - description: "Initialize an Odoo ERP project from a PRD document"
       - allowed-tools: Read, Bash, Write, Task, AskUserQuestion
       - execution_context: `@~/.claude/amil/workflows/new-erp.md`
       - process: "Execute the new-erp workflow end-to-end."

    2. Create `agents/amil-erp-decomposer.md` -- Module Boundary Analyzer agent:
       - Role: Analyze a PRD to identify functional domains and propose Odoo module boundaries
       - Input context: PRD text + existing_modules list from config
       - Instructions: Identify each functional domain, propose module names using `uni_` prefix, list models per module using dot notation (uni.institution, uni.student, etc.), identify base Odoo depends (base, mail, account, etc.), estimate complexity (low/medium/high)
       - Output format: Write JSON to `.planning/research/module-boundaries.json` matching schema: `{ "modules": [{ "name": string, "description": string, "models": string[], "base_depends": string[], "estimated_complexity": string }] }`
       - Include the EXACT JSON schema in the agent prompt so output is deterministic
       - Add instruction: "If PRD lacks detail for a domain, output with estimated_complexity: 'unknown' and note in description"
       - Module naming convention: all custom modules use `uni_` prefix

    3. Create `agents/amil-module-researcher.md` -- OCA Registry Checker agent:
       - Role: Check if OCA/standard Odoo already has modules covering each domain
       - Input context: PRD text + existing_modules list from config
       - Instructions: For each domain in the PRD, use Claude training knowledge (NO web fetch, NO brave_search) to identify existing OCA or standard Odoo modules. Recommend: build_new (no existing module), extend (existing module can be extended), skip (existing module covers requirements fully)
       - Output format: Write JSON to `.planning/research/oca-analysis.json` matching schema: `{ "findings": [{ "domain": string, "oca_module": string|null, "odoo_module": string|null, "recommendation": "build_new"|"extend"|"skip", "reason": string }] }`
       - Include the EXACT JSON schema in the agent prompt
       - Emphasize: Use training data only, do not attempt web searches
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && test -f commands/amil/new-erp.md && test -f agents/amil-erp-decomposer.md && test -f agents/amil-module-researcher.md && grep -q "amil:new-erp" commands/amil/new-erp.md && grep -q "module-boundaries.json" agents/amil-erp-decomposer.md && grep -q "oca-analysis.json" agents/amil-module-researcher.md && echo "PASS"</automated>
  </verify>
  <done>Command file references workflow, both agent files contain exact JSON output schemas, agent naming follows amil-*.md pattern</done>
</task>

<task type="auto">
  <name>Task 2: Create new-erp workflow with Stage A (questions) and Stage B (agents)</name>
  <files>amil/workflows/new-erp.md</files>
  <action>
    Create `amil/workflows/new-erp.md` with the complete workflow. Structure:

    **Preamble:**
    - Description of the workflow purpose
    - Reference to CLAUDE.md rules (CJS, zero npm deps, atomic writes)

    **Stage A: Odoo Config Collection**
    - Step 1: Ensure `.planning/config.json` exists (call `config ensure-section`)
    - Step 2: Ask 7 sequential config questions using the tool/interaction pattern. Each question:
      - Q1: "Which Odoo version?" -- options: 17.0, 18.0 -- default: 17.0 -- write: `config-set odoo.version`
      - Q2: "Multi-company setup (separate company per campus)?" -- options: yes/no -- default: no -- write: `config-set odoo.multi_company`
      - Q3: "Localization?" -- options: pk, sa, ae, none -- default: pk -- write: `config-set odoo.localization`
      - Q4: "Existing Odoo modules being extended?" -- free text, comma-separated or "none" -- default: none -- write: `config-set odoo.existing_modules`
      - Q5: "LMS integration?" -- options: canvas, moodle, none -- default: canvas -- write: `config-set odoo.canvas_integration`
      - Q6: "Notification channels?" -- multi-select from: email, whatsapp, sms -- write: `config-set odoo.notification_channels` as JSON array
      - Q7: "Deployment target?" -- options: single, multi -- default: single -- write: `config-set odoo.deployment_target`
    - Each question MUST write to config.json immediately after answer (not batched)
    - Store Q4 answer in a variable for passing to agents

    **Stage B: PRD Analysis**
    - Step 1: Check for `.planning/PRD.md`. If missing, ask user to provide PRD content or file path. If user provides content, write it to `.planning/PRD.md`.
    - Step 2: Read PRD content and existing_modules from config
    - Step 3: Create `.planning/research/` directory if it does not exist
    - Step 4: Spawn 4 parallel Task() agents:
      - Agent 1 (dedicated): `Task(prompt="Analyze this PRD... [PRD text] [existing_modules]...", subagent_type="amil-erp-decomposer", description="Module boundary analysis")`
      - Agent 2 (dedicated): `Task(prompt="Check OCA registry for... [PRD text] [existing_modules]...", subagent_type="amil-module-researcher", description="OCA registry check")`
      - Agent 3 (inline): `Task(prompt="Map dependencies between modules... [PRD text] [existing_modules]... Write JSON to .planning/research/dependency-map.json with schema { dependencies: [{ module, depends_on: [], reason }] }", description="Dependency mapping")`
      - Agent 4 (inline): `Task(prompt="Identify computation chains... [PRD text] [existing_modules]... Write JSON to .planning/research/computation-chains.json with schema { chains: [{ name, description, steps: [], cross_module: boolean }] }", description="Computation chain identification")`
    - All 4 launched in parallel, workflow waits for all to complete
    - Step 5: Validate that all 4 JSON output files exist in `.planning/research/`. If any missing, report which agent failed and offer retry.

    **Stage C placeholder comment:**
    - Add a comment block: "Stage C: Merge, Approval, and ROADMAP Generation -- implemented in next plan iteration"
    - This signals to the executor that the workflow is not yet complete

    Important:
    - Use `$HOME/.claude/amil/bin/amil-tools.cjs` for config-set calls
    - Agent prompts must include the EXACT JSON schema they should output
    - Agent prompts must include the PRD text and existing_modules as context
    - Inline Task() agents (3 and 4) do NOT have a subagent_type -- they use the default model
    - The workflow file is markdown with structured sections, not code
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && test -f amil/workflows/new-erp.md && grep -q "Stage A" amil/workflows/new-erp.md && grep -q "Stage B" amil/workflows/new-erp.md && grep -q "config-set odoo.version" amil/workflows/new-erp.md && grep -q "amil-erp-decomposer" amil/workflows/new-erp.md && grep -q "amil-module-researcher" amil/workflows/new-erp.md && grep -q "dependency-map.json" amil/workflows/new-erp.md && grep -q "computation-chains.json" amil/workflows/new-erp.md && echo "PASS"</automated>
  </verify>
  <done>Workflow contains Stage A with 7 questions writing to config, Stage B with 4 parallel Task() agent spawns, PRD.md check, output validation, Stage C placeholder</done>
</task>

</tasks>

<verification>
- `commands/amil/new-erp.md` exists with correct frontmatter and execution_context
- `amil/workflows/new-erp.md` contains Stage A (7 questions) and Stage B (4 agents)
- `agents/amil-erp-decomposer.md` contains module boundary analysis instructions with exact JSON schema
- `agents/amil-module-researcher.md` contains OCA registry check instructions with exact JSON schema
- Agent frontmatter test passes: `node --test tests/agent-frontmatter.test.cjs`
- All 7 config questions map to correct odoo.* keys with validation from Plan 01
</verification>

<success_criteria>
- /amil:new-erp command file exists and references workflow
- Workflow has complete Stage A (7 questions) and Stage B (4 parallel agents)
- Both dedicated agent files contain exact JSON output schemas
- Agent naming follows amil-*.md convention
</success_criteria>

<output>
After completion, create `.planning/phases/02-prd-decomposition-and-erp-init/02-02-SUMMARY.md`
</output>

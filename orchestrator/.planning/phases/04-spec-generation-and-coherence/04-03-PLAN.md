---
phase: 04-spec-generation-and-coherence
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - commands/amil/plan-module.md
  - amil/workflows/plan-module.md
  - tests/plan-module.test.cjs
autonomous: false
requirements: [SPEC-01, SPEC-04, SPEC-07, SPEC-08]

must_haves:
  truths:
    - "Running /amil:plan-module {module} produces spec.json with _available_models from tiered registry"
    - "Coherence report is presented to human before approval"
    - "Human can approve, revise, or abort the spec"
    - "On approval, module status transitions to spec_approved"
  artifacts:
    - path: "commands/amil/plan-module.md"
      provides: "Slash command entry point for plan-module"
      contains: "plan-module"
    - path: "amil/workflows/plan-module.md"
      provides: "10-step workflow orchestrating researcher, spec generator, coherence, reviewer, approval"
      min_lines: 100
    - path: "tests/plan-module.test.cjs"
      provides: "Structural validation tests for plan-module command and workflow"
      min_lines: 60
  key_links:
    - from: "amil/workflows/plan-module.md"
      to: "amil/bin/lib/coherence.cjs"
      via: "CLI invocation"
      pattern: "amil-tools.cjs coherence check"
    - from: "amil/workflows/plan-module.md"
      to: "agents/amil-spec-generator.md"
      via: "Task() with subagent_type"
      pattern: "subagent_type.*spec-generator"
    - from: "amil/workflows/plan-module.md"
      to: "agents/amil-spec-reviewer.md"
      via: "Task() with subagent_type"
      pattern: "subagent_type.*spec-reviewer"
    - from: "amil/workflows/plan-module.md"
      to: "amil/bin/lib/module-status.cjs"
      via: "CLI invocation"
      pattern: "module-status transition.*spec_approved"
    - from: "amil/workflows/plan-module.md"
      to: "amil/bin/lib/registry.cjs"
      via: "tieredRegistryInjection"
      pattern: "tieredRegistryInjection\\|tiered-registry"
---

<objective>
Create the plan-module slash command and 10-step workflow that orchestrates the full spec generation pipeline: context loading, researcher agent, tiered registry injection, spec generator agent, coherence check, reviewer agent presentation, and human approval. Plus structural validation tests.

Purpose: This is the user-facing command that ties together all Phase 4 components into a cohesive pipeline. It transforms module CONTEXT.md into an approved spec.json ready for belt generation.

Output: Slash command file + workflow file + structural test file
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-spec-generation-and-coherence/04-CONTEXT.md
@.planning/phases/04-spec-generation-and-coherence/04-RESEARCH.md
@.planning/phases/04-spec-generation-and-coherence/04-01-SUMMARY.md
@.planning/phases/04-spec-generation-and-coherence/04-02-SUMMARY.md

<interfaces>
<!-- Key interfaces from Phase 4 Plans 01 and 02 -->

From amil/bin/lib/coherence.cjs (created in Plan 01):
```javascript
function runAllChecks(spec, registry) // returns {status, checks[]}
function cmdCoherenceCheck(cwd, specPath, registryPath, raw) // CLI entry
module.exports = { runAllChecks, checkMany2oneTargets, checkDuplicateModels, checkComputedDepends, checkSecurityGroups, cmdCoherenceCheck, BASE_ODOO_MODELS }
```

CLI coherence invocation:
```bash
node "$HOME/.claude/amil/bin/amil-tools.cjs" coherence check --spec <path> --registry <path> --raw --cwd "$(pwd)"
```

From agents/amil-spec-generator.md (created in Plan 02):
- subagent_type: "amil-spec-generator"
- Input: module name, CONTEXT.md, RESEARCH.md, decomposition entry, config odoo block, tiered registry JSON
- Output: writes spec.json to .planning/modules/{module}/spec.json

From agents/amil-spec-reviewer.md (created in Plan 02):
- subagent_type: "amil-spec-reviewer"
- Input: coherence report JSON, spec.json path, module name
- Output: human-readable summary (printed, not written to file)

From amil/bin/lib/registry.cjs:
```javascript
function tieredRegistryInjection(cwd, moduleName) // returns {direct, transitive, rest}
function readRegistryFile(cwd) // returns parsed registry
```

From amil/bin/lib/module-status.cjs:
```javascript
function cmdModuleStatusTransition(cwd, moduleName, newStatus, raw) // transitions status
function readStatusFile(cwd) // returns parsed module_status.json
```

CLI module-status invocation:
```bash
node "$HOME/.claude/amil/bin/amil-tools.cjs" module-status transition "${MODULE}" spec_approved --raw --cwd "$(pwd)"
```

From commands/amil/discuss-module.md (pattern to follow):
```yaml
---
name: amil:discuss-module
description: Run interactive module discussion to capture design decisions
argument-hint: "{module_name}"
allowed-tools:
  - Read
  - Bash
  - Write
  - Task
  - AskUserQuestion
---
```

From amil/workflows/discuss-module.md (pattern to follow):
- Uses numbered steps (## Step N: ...)
- Loads module data first, validates status
- Spawns agents via Task(subagent_type="...")
- Uses AskUserQuestion for human interaction
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plan-module slash command and workflow</name>
  <files>commands/amil/plan-module.md, amil/workflows/plan-module.md</files>
  <action>
    1. Create commands/amil/plan-module.md following the exact pattern of discuss-module.md:
       - Frontmatter: name "amil:plan-module", description about generating spec.json, argument-hint "{module_name}"
       - allowed-tools: Read, Bash, Write, Task, AskUserQuestion
       - Context section explaining what the command does
       - Objective: "Generate a complete spec.json for the specified module from CONTEXT.md and registry context"
       - After this command note: "Review spec.json, then run `/amil:generate-module {module}` to generate code"
       - execution_context pointing to @~/.claude/amil/workflows/plan-module.md

    2. Create amil/workflows/plan-module.md with the locked 10-step flow:

       ## Step 1: Validate Module Status
       - Read module_status.json via: `node "$HOME/.claude/amil/bin/amil-tools.cjs" module-status get "${MODULE}" --raw --cwd "$(pwd)"`
       - Verify status is "planned" (or allow re-run if "spec_approved" with user confirmation to overwrite)
       - If module not found, error with helpful message

       ## Step 2: Check Existing Spec
       - Check if `.planning/modules/${MODULE}/spec.json` exists
       - If exists, use AskUserQuestion: "spec.json already exists. Overwrite, skip, or abort?"

       ## Step 3: Load Module Context
       - Read `.planning/modules/${MODULE}/CONTEXT.md`
       - If missing, error: "Run /amil:discuss-module ${MODULE} first"

       ## Step 4: Load Decomposition Entry
       - Read `.planning/decomposition.json` and find the entry for this module
       - Extract: module name, description, depends list, tier

       ## Step 5: Spawn Researcher Agent
       - Spawn Task(subagent_type="amil-module-researcher") with module context
       - Agent produces `.planning/modules/${MODULE}/RESEARCH.md`
       - Read the produced RESEARCH.md

       ## Step 6: Build Tiered Registry
       - If model_registry.json exists and is non-empty:
         Run `node "$HOME/.claude/amil/bin/amil-tools.cjs" registry tiered-injection "${MODULE}" --raw --cwd "$(pwd)"`
       - If registry is empty or missing (first module in project): use empty tiered result `{"direct": {}, "transitive": {}, "rest": []}`

       ## Step 7: Spawn Spec Generator Agent
       - Spawn Task(subagent_type="amil-spec-generator") with:
         - Module name
         - CONTEXT.md content
         - RESEARCH.md content
         - Decomposition entry (name, description, depends, tier)
         - Config odoo block (read from .planning/config.json)
         - Tiered registry JSON (for _available_models section)
       - Agent writes spec.json to `.planning/modules/${MODULE}/spec.json`
       - Read and validate the produced spec.json (valid JSON, has all 12 top-level keys)

       ## Step 8: Run Coherence Check
       - Run: `node "$HOME/.claude/amil/bin/amil-tools.cjs" coherence check --spec ".planning/modules/${MODULE}/spec.json" --registry ".planning/model_registry.json" --raw --cwd "$(pwd)"`
       - Parse the JSON coherence report
       - Save report to `.planning/modules/${MODULE}/coherence-report.json`

       ## Step 9: Spawn Reviewer Agent (Presentation)
       - Spawn Task(subagent_type="amil-spec-reviewer") with:
         - Coherence report JSON
         - Path to spec.json
         - Module name
       - Agent presents human-readable summary

       ## Step 10: Human Approval
       - If coherence status is "fail":
         Use AskUserQuestion: "Coherence check found violations. Options: approve_anyway, revise (re-run from Step 5), abort"
       - If coherence status is "pass":
         Use AskUserQuestion: "Spec looks good. Options: approve, revise, abort"
       - On "approve" or "approve_anyway":
         Run: `node "$HOME/.claude/amil/bin/amil-tools.cjs" module-status transition "${MODULE}" spec_approved --raw --cwd "$(pwd)"`
         Print confirmation message
       - On "revise": Go back to Step 5 (researcher)
       - On "abort": Stop workflow, no status change

    IMPORTANT: Each step should have clear error handling. If any step fails, present a clear error and stop.
    IMPORTANT: Use the exact CLI invocation patterns shown in the interfaces section.
    IMPORTANT: For Task() spawning, use the subagent_type pattern from discuss-module.md workflow.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && test -f commands/amil/plan-module.md && test -f amil/workflows/plan-module.md && grep -q "plan-module" commands/amil/plan-module.md && grep -q "coherence check" amil/workflows/plan-module.md && grep -q "spec_approved" amil/workflows/plan-module.md && grep -q "spec-generator" amil/workflows/plan-module.md && grep -q "spec-reviewer" amil/workflows/plan-module.md && echo "PASS"</automated>
  </verify>
  <done>plan-module command and workflow exist with all 10 steps. Workflow references coherence CLI, spec generator agent, spec reviewer agent, and module status transition. Command has correct frontmatter with all required tools.</done>
</task>

<task type="auto">
  <name>Task 2: Create plan-module structural validation tests</name>
  <files>tests/plan-module.test.cjs</files>
  <action>
    Create tests/plan-module.test.cjs using node:test and node:assert, following the pattern in tests/commands.test.cjs and tests/agent-frontmatter.test.cjs.

    Tests to include:

    1. **Command file structure (SPEC-01):**
       - commands/amil/plan-module.md exists
       - Contains frontmatter with name "amil:plan-module"
       - Contains argument-hint with "module_name"
       - References the workflow file (plan-module.md)

    2. **Workflow file structure (SPEC-01):**
       - amil/workflows/plan-module.md exists
       - Contains all 10 steps (grep for "Step 1" through "Step 10")
       - References coherence check CLI: "coherence check"
       - References spec-generator subagent_type
       - References spec-reviewer subagent_type
       - References module-status transition to spec_approved
       - References tiered-registry or tieredRegistryInjection

    3. **Spec schema coverage (SPEC-03):**
       - Workflow or command mentions all 12 spec.json sections (models, business_rules, computation_chains, workflow, view_hints, reports, notifications, cron_jobs, security, portal, api_endpoints, _available_models)
       - Check in EITHER workflow file OR spec generator agent (since generator is where schema is enforced)

    4. **Tiered registry injection (SPEC-04):**
       - Workflow mentions tiered registry injection step
       - Workflow mentions _available_models

    Use fs.readFileSync to read the markdown files and assert content patterns. Each test should be a separate `test()` or `describe()` block.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && node --test tests/plan-module.test.cjs && npm test</automated>
  </verify>
  <done>plan-module.test.cjs validates command file structure, workflow 10-step completeness, spec schema section coverage, and tiered registry injection references. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete plan-module pipeline</name>
  <files>commands/amil/plan-module.md, amil/workflows/plan-module.md</files>
  <action>Human verifies the complete plan-module pipeline works end-to-end.</action>
  <what-built>Complete plan-module pipeline: slash command, 10-step workflow, coherence checker (Plan 01), spec generator agent (Plan 02), spec reviewer agent (Plan 02), structural tests</what-built>
  <how-to-verify>
    1. Review the workflow at `amil/workflows/plan-module.md` -- verify all 10 steps are present and logical
    2. Verify the command file at `commands/amil/plan-module.md` references the workflow
    3. Run full test suite: `cd /home/inshal-rauf/Odoo_GSD && npm test` -- all tests should pass
    4. Optional: Try running `/amil:plan-module` on a test module if test data is available
  </how-to-verify>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && npm test</automated>
  </verify>
  <done>Human confirms workflow is correct and complete. All tests pass.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `node --test tests/plan-module.test.cjs` -- structural tests pass for command + workflow
2. `test -f commands/amil/plan-module.md` -- command file exists
3. `test -f amil/workflows/plan-module.md` -- workflow file exists
4. Workflow contains all 10 steps with correct CLI invocations
5. `npm test` -- full suite passes
</verification>

<success_criteria>
- /amil:plan-module command registered and routes to workflow
- Workflow implements the locked 10-step flow from CONTEXT.md
- Tiered registry injection feeds _available_models into spec.json
- Coherence check runs via CLI and report is presented to human
- Human approval transitions module status to spec_approved
- Structural tests validate command, workflow, spec schema, and registry injection
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-spec-generation-and-coherence/04-03-SUMMARY.md`
</output>

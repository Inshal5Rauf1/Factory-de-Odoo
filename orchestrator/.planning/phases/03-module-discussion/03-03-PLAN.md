---
phase: 03-module-discussion
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - commands/amil/discuss-module.md
  - amil/workflows/discuss-module.md
autonomous: true
requirements:
  - DISC-01
  - DISC-05

must_haves:
  truths:
    - "Running /amil:discuss-module {module} loads the workflow which detects module type, loads the correct question template, and spawns the questioner agent"
    - "Workflow validates module exists in module_status.json at 'planned' status before proceeding"
    - "Questioner agent writes structured CONTEXT.md to .planning/modules/{module}/CONTEXT.md"
  artifacts:
    - path: "commands/amil/discuss-module.md"
      provides: "Slash command for discuss-module"
      contains: "amil:discuss-module"
      min_lines: 15
    - path: "amil/workflows/discuss-module.md"
      provides: "Workflow orchestrating type detection, template loading, agent spawning"
      contains: "amil-module-questioner"
      min_lines: 80
  key_links:
    - from: "commands/amil/discuss-module.md"
      to: "amil/workflows/discuss-module.md"
      via: "execution_context reference"
      pattern: "workflows/discuss-module\\.md"
    - from: "amil/workflows/discuss-module.md"
      to: "agents/amil-module-questioner.md"
      via: "Task() spawn with subagent_type"
      pattern: 'subagent_type="amil-module-questioner"'
    - from: "amil/workflows/discuss-module.md"
      to: "amil/references/module-questions.json"
      via: "Read tool to load question template"
      pattern: "module-questions\\.json"
---

<objective>
Create the discuss-module command and workflow that wire together the question templates and questioner agent into an interactive module discussion flow.

Purpose: This is the user-facing entry point. The command triggers the workflow, which validates the module, detects its type, loads the correct question template, and spawns the questioner agent for interactive Q&A. The agent writes the final CONTEXT.md output.

Output: `commands/amil/discuss-module.md` (command), `amil/workflows/discuss-module.md` (workflow)
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-module-discussion/03-CONTEXT.md
@.planning/phases/03-module-discussion/03-RESEARCH.md

@commands/amil/new-erp.md
@amil/workflows/new-erp.md

<interfaces>
<!-- From Plan 01: module-questions.json schema -->
Each type entry in module-questions.json:
```json
{
  "fee": {
    "questions": [
      { "id": "fee_field_type", "question": "...", "options": ["monetary", "float"], "default": "monetary", "context": "..." }
    ],
    "context_hints": ["Check config.json for localization..."]
  }
}
```

<!-- From Plan 02: questioner agent -->
Agent name: amil-module-questioner
Agent input: Module type + question template + decomposition entry + config odoo block
Agent output: .planning/modules/{module}/CONTEXT.md

<!-- Module type detection lookup (inline in workflow) -->
```javascript
const MODULE_TYPES = {
  'uni_core': 'core', 'uni_student': 'student', 'uni_fee': 'fee',
  'uni_exam': 'exam', 'uni_faculty': 'faculty', 'uni_hr': 'hr',
  'uni_timetable': 'timetable', 'uni_notification': 'notification',
  'uni_portal': 'portal'
};
```

<!-- CLI commands for reading state -->
module-status get: `node amil/bin/amil-tools.cjs module-status get {module} --raw --cwd "$(pwd)"`
config-get odoo: `node amil/bin/amil-tools.cjs config-get odoo --raw --cwd "$(pwd)"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create discuss-module command file</name>
  <files>commands/amil/discuss-module.md</files>
  <action>
Create `commands/amil/discuss-module.md` following the exact pattern from `commands/amil/new-erp.md`.

**Frontmatter:**
```yaml
---
name: amil:discuss-module
description: Run interactive module discussion to capture design decisions
argument-hint: "{module_name}"
allowed-tools:
  - Read
  - Bash
  - Write
  - Task
  - AskUserQuestion
---
```

**Body sections:**

`<context>`:
- Runs a module-type-aware Q&A session producing structured CONTEXT.md.
- **Requires:** Module initialized in module_status.json (status: planned)
- **Produces:** `.planning/modules/{module}/CONTEXT.md`

`<objective>`:
- Capture module design decisions through interactive discussion.
- **After this command:** Review CONTEXT.md, then run `/amil:plan-module {module}` to generate spec.

`<execution_context>`:
- `@~/.claude/amil/workflows/discuss-module.md`

`<process>`:
- Execute the discuss-module workflow end-to-end for the specified module.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && node --test tests/discuss-module.test.cjs --test-name-pattern "CMD" 2>&1 | tail -5</automated>
  </verify>
  <done>Command file exists with correct frontmatter (name: amil:discuss-module, allowed-tools includes Task and AskUserQuestion), references discuss-module workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Create discuss-module workflow</name>
  <files>amil/workflows/discuss-module.md</files>
  <action>
Create `amil/workflows/discuss-module.md` following the structure of `amil/workflows/new-erp.md`.

**Content structure:**

```markdown
# Workflow: discuss-module

Run an interactive module discussion to capture design decisions for a specific Odoo module.

**Rules:** CJS tooling, zero npm deps, atomic writes via `amil-tools.cjs`.

---

## Prerequisites

The module argument `{MODULE}` is provided by the user when invoking `/amil:discuss-module {module_name}`.

---

## Step 1: Validate Module Exists

Check that the module exists in module_status.json and is at "planned" status.

\```bash
MODULE_STATUS=$(node "$HOME/.claude/amil/bin/amil-tools.cjs" module-status get "${MODULE}" --raw --cwd "$(pwd)" 2>&1)
\```

- If command fails or returns empty: show error "Module '${MODULE}' not found in module_status.json. Run /amil:new-erp first." and STOP.
- Parse the JSON result. If `.status` is not "planned": show error "Module '${MODULE}' is at status '${status}', expected 'planned'. Discussion is only for modules at 'planned' status." and STOP.

## Step 2: Check for Existing CONTEXT.md

\```bash
ls .planning/modules/${MODULE}/CONTEXT.md 2>/dev/null
\```

- If file exists and has more than 5 lines of content: use AskUserQuestion to ask "CONTEXT.md already exists for ${MODULE}. What would you like to do?" with options: "overwrite" (start fresh), "skip" (keep existing, end workflow), "update" (append to existing).
- If "skip": STOP workflow (success, no changes).
- If "overwrite" or file doesn't exist: continue.
- If "update": note this in the agent prompt so it reads existing content first.

## Step 3: Detect Module Type

Use a lookup table to detect the module type from the module name:

MODULE_TYPES lookup:
- uni_core -> core
- uni_student -> student
- uni_fee -> fee
- uni_exam -> exam
- uni_faculty -> faculty
- uni_hr -> hr
- uni_timetable -> timetable
- uni_notification -> notification
- uni_portal -> portal

Detection logic:
1. Exact match against lookup keys
2. If no exact match, check if module name starts with any key followed by '_'
3. If no match at all, set type to null

If type detected: use AskUserQuestion to confirm: "Detected module type: **{type}**. Correct? (yes / change)"
- If "yes" or confirmed: proceed with detected type
- If "change" or user provides different type: use user's type

If type is null: use AskUserQuestion to ask user to select from: core, student, fee, exam, faculty, hr, timetable, notification, portal, generic

## Step 4: Load Question Template

Read the question template for the detected type from module-questions.json:

\```bash
# Read via the Read tool (not Bash cat)
Read amil/references/module-questions.json
\```

Extract ONLY the relevant type's entry (questions array + context_hints). If type not found in JSON, fall back to "generic".

## Step 5: Load Module Metadata

\```bash
# Get decomposition entry for the module
Read .planning/research/decomposition.json
# Extract the module's entry: models, depends, complexity

# Get odoo config block
ODOO_CONFIG=$(node "$HOME/.claude/amil/bin/amil-tools.cjs" config-get odoo --raw --cwd "$(pwd)" 2>/dev/null || echo '{}')
\```

## Step 6: Spawn Questioner Agent

Ensure module directory exists:
\```bash
mkdir -p ".planning/modules/${MODULE}/"
\```

Spawn the questioner agent via Task():

\```
Task(
  prompt="You are discussing module design for ${MODULE_NAME} (type: ${MODULE_TYPE}).

MODULE METADATA:
---
Name: ${MODULE_NAME}
Type: ${MODULE_TYPE}
Models: ${MODELS_LIST}
Dependencies: ${DEPENDS_LIST}
Complexity: ${COMPLEXITY}
---

ODOO CONFIG:
---
${ODOO_CONFIG_JSON}
---

QUESTION TEMPLATE:
---
${QUESTIONS_JSON_FOR_TYPE}
---

CONTEXT HINTS:
---
${CONTEXT_HINTS}
---

${UPDATE_MODE_NOTE if applicable}

Follow your agent instructions to run the Q&A session and write CONTEXT.md to:
.planning/modules/${MODULE_NAME}/CONTEXT.md",
  subagent_type="amil-module-questioner",
  description="Module discussion: ${MODULE_NAME}"
)
\```

## Step 7: Verify Output

After agent completes, verify the CONTEXT.md was written:

\```bash
ls -la ".planning/modules/${MODULE}/CONTEXT.md"
\```

If file exists and has content: report success.
If file missing: report error -- agent did not write output.

## Completion

Report to user:
- "Discussion complete for ${MODULE}."
- "Output: .planning/modules/${MODULE}/CONTEXT.md"
- "Next step: /amil:plan-module ${MODULE}"
```

**Important details:**
- Use `subagent_type="amil-module-questioner"` in the Task() call (NOT "First, read agent .md" workaround)
- Only pass the RELEVANT type's questions to the agent (not the entire module-questions.json)
- Handle edge cases: module not found, already discussed, config missing
- The workflow does NOT write CONTEXT.md -- the agent does
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && node --test tests/discuss-module.test.cjs --test-name-pattern "WF|CMD" 2>&1 | tail -5</automated>
  </verify>
  <done>Workflow file exists with 7 steps (validate, check existing, detect type, load template, load metadata, spawn agent, verify output). References module-questions.json, uses subagent_type="amil-module-questioner", and validates module status before proceeding. All discuss-module tests pass.</done>
</task>

</tasks>

<verification>
- `node --test tests/discuss-module.test.cjs` -- ALL tests pass (templates, type detection, command, workflow, agents)
- `node --test tests/agent-frontmatter.test.cjs` -- still passes with new questioner agent
- `npm test` -- full suite passes (no regressions)
- `grep 'subagent_type="amil-module-questioner"' amil/workflows/discuss-module.md` -- returns match
- `grep 'module-questions.json' amil/workflows/discuss-module.md` -- returns match
</verification>

<success_criteria>
- Command file routes to workflow correctly
- Workflow validates module status, detects type, loads template, spawns agent
- All discuss-module.test.cjs tests pass (DISC-01 through DISC-05, AGNT-01)
- Full test suite (npm test) passes with no regressions
- No "First, read agent .md" workaround pattern used
</success_criteria>

<output>
After completion, create `.planning/phases/03-module-discussion/03-03-SUMMARY.md`
</output>
